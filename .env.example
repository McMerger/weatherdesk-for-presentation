# WeatherDesk Environment Variables
# Copy this file to .env.local for local development

# Backend API URL - Points to Kotlin/Ktor backend (REQUIRED)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Development Configuration
# The Kotlin backend fetches REAL weather data from OpenMeteo API
# OpenMeteo is completely free and requires NO API keys!

# Legacy note: OPENWEATHER_API_KEY is not used in this version
# The app now uses OpenMeteo API via the Kotlin backend
